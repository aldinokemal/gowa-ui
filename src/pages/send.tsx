import { PlaceholderPage } from '@/components/shared/placeholder-page'

export default function SendPage() {
  return (
    <PlaceholderPage
      title="Send"
      description="Message, image, file, video, sticker, contact, location, audio, poll, link, presence."
      milestone="M2"
    />
  )
}
